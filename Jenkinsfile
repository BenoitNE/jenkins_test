pipeline {
  
          agent any

          tools {
            maven 'maven'
          }

          stages{
            stage('Get from git project'){
  steps {
    git https://github.com/BenoitNE/jenkins_test.git'
  }
            }
}
}